#! Generated by VisualDCT v2.7.1-SNAPSHOT

# % macro, P, Device Prefix
# % macro, R, Device Suffix
# % macro, DTYP, Device Type
# % macro, OUT, Output link
# % macro, SCAN, Scan Rate
# % macro, HIGH, Seconds to hold signal high
# % macro, ZNAM, Name for zero state
# % macro, ONAM, Name for one state
# % macro, VAL, Initial value
# % macro, DESC, Description
# % macro, PINI, Process at startup ("YES"/"NO")
# % macro, name, Object name and gui association name
# % macro, gda_name, GDA Name
# % macro, gda_desc, GDA Description
# % macro, archiver_rate, epics archiver configuration
# This associates an edm screen with the template
# % gui, $(name=), edmembed, bo_embed.edl, P=$(P),R=$(R)
# % gui, $(name=), boyembed, userIOApp_opi/bo_embed.opi, P=$(P),R=$(R), label=$(DESC)
# This makes the component icon reflect the severity
# % gui, $(name=), sevr
#% gdatag,template,simpleBinary,$(gda_name=),$(gda_desc=)
#% gdatag,binary,rw,$(gda_name=),RECORD
#% archiver $(archiver_rate=10)
#% autosave 1 VAL
record(bo, "$(P)$(R)") {
  field(DTYP, "$(DTYP)")
  field(OUT, "$(OUT)")
  field(SCAN, "$(SCAN)")
  field(HIGH, "$(HIGH)")
  field(ZNAM, "$(ZNAM)")
  field(ONAM, "$(ONAM)")
  field(VAL, "$(VAL=0)")
  field(DESC, "$(DESC=Binary Output)")
  field(PINI, "$(PINI=NO)")
}
