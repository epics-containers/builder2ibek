#! Generated by VisualDCT v2.7.1-SNAPSHOT

# % macro, P, Device Prefix
# % macro, R, Device Suffix
# % macro, DTYP, Device Type
# % macro, OUT, Output link
# % macro, SCAN, Scan Rate
# % macro, PREC, Precision
# % macro, EGUL, EGU low scale
# % macro, EGUF, EGU full scale
# % macro, DRVH, Drive high limit
# % macro, DRVL, Drive low limit
# % macro, EGU, EGU
# % macro, PINI, Process on iocInit
# % macro, DESC, Description
# % macro, LINR, LINR
# % macro, name, Object name and gui association name
# % macro, gda_name, GDA Name
# % macro, gda_desc, GDA Description
# % macro, archiver_rate, epics archiver configuration
# This associates an edm screen with the template
# % gui, $(name=), edmembed, ao_embed.edl, P=$(P),R=$(R)
# % gui, $(name=), boydetail, userIOApp_opi/ao_detail.opi, P=$(P),R=$(R), desc=$(DESC), DESC=$(DESC)
# % gui, $(name=), boyembed, userIOApp_opi/ao_embed_box.opi, P=$(P),R=$(R), desc=$(DESC), DESC=$(DESC)
# % gui, $(name=), boyembed, userIOApp_opi/ao_embed.opi, P=$(P),R=$(R), desc=$(DESC), DESC=$(DESC)
# % gui, $(name=), demand, Set Value, $(P)$(R)
# % gui, $(name=), groupHeading, Settings
# % gui, $(name=), demand, EGU, $(P)$(R).EGU
# % gui, $(name=), readback, EGU, $(P)$(R).EGU
# % gui, $(name=), demand, EGU Low, $(P)$(R).EGUL
# % gui, $(name=), demand, EGU Full, $(P)$(R).EGUF
# % gui, $(name=), demand, Drive Low, $(P)$(R).DRVL
# % gui, $(name=), demand, Drive High, $(P)$(R).DRVH
# % gui, $(name=), demand, Precision, $(P)$(R).PREC
# % gui, $(name=), enum, Conversion Type, $(P)$(R).LINR
# This makes the component icon reflect the severity
# % gui, $(name=), sevr
#% gdatag,template,simplePv,$(gda_name=),$(gda_desc=)
#% gdatag,pv,rw,$(gda_name=),RECORD
#% archiver $(archiver_rate=10)
#% autosave 1 VAL
record(ao, "$(P)$(R)") {
  field(DTYP, "$(DTYP)")
  field(OUT, "$(OUT)")
  field(SCAN, "$(SCAN)")
  field(LINR, "$(LINR)")
  field(EGUF, "$(EGUF)")
  field(EGUL, "$(EGUL)")
  field(DRVH, "$(DRVH)")
  field(DRVL, "$(DRVL)")
  field(DESC, "$(DESC=Analogue Output)")
  field(EGU, "$(EGU=V)")
  field(PINI, "$(PINI=NO)")
  field(PREC, "$(PREC=)")
}
