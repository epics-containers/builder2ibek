#! Generated by VisualDCT v2.7.1-SNAPSHOT

#% gdatag,template,simplePv,$(gda_name=),$(gda_desc=)
#% gdatag,pv,rw,$(gda_name=),RECORD
#% archiver 10
#% autosave 1 VAL
record(ao, "$(P)$(R)") {
  field(DTYP, "Soft Channel")
  field(SCAN, "$(SCAN)")
  field(LINR, "$(LINR)")
  field(EGUF, "$(EGUF)")
  field(EGUL, "$(EGUL)")
  field(DRVH, "$(DRVH)")
  field(DRVL, "$(DRVL)")
  field(EGU, "$(EGU=)")
  field(PREC, "$(PREC=)")
  field(DESC, "$(DESC=)")
  field(UDF, "0")
}
