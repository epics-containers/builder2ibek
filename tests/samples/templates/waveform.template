#! Generated by VisualDCT v2.7.1-SNAPSHOT

#% gdatag,template,simpleArray,$(gda_name=),$(gda_desc=)
#% gdatag,array,ro,$(gda_name=),RECORD
# This associates an edm screen with the template
# % gui, $(name=), edm, waveform_small.edl, P=$(P),R=$(R)
record(waveform, "$(P)$(R)") {
  field(DTYP, "$(DTYP)")
  field(FTVL, "FLOAT")
  field(NELM, "$(NELM)")
  field(INP, "$(INP)")
  field(SCAN, "$(SCAN)")
  field(DESC, "$(DESC=Array)")
}
