#! Generated by VisualDCT v2.7.1-SNAPSHOT

# % macro, P, Device Prefix
# % macro, R, Device Suffix
# % macro, DTYP, Device Type
# % macro, OUT, Output link
# % macro, SCAN, Scan Rate
# % macro, HIGH, Seconds to hold signal high
# % macro, ZNAM, Name for zero state
# % macro, ONAM, Name for one state
# % macro, VAL, Initial value
# % macro, DESC, Description
# % macro, PINI, Process at startup ("YES"/"NO")
# % macro, name, Object name and gui association name
# % macro, gda_name, GDA Name
# % macro, gda_desc, GDA Description
# % macro, archiver_rate, epics archiver configuration
# This associates an edm screen with the template
# % gui, USER.B1, edmembed, bo_embed.edl, P=$(dom,undefined)-EA-USER-01:B1,R=
# % gui, USER.B1, boyembed, userIOApp_opi/bo_embed.opi, P=$(dom,undefined)-EA-USER-01:B1,R=, label=Binary Output
# This makes the component icon reflect the severity
# % gui, USER.B1, sevr
#% gdatag,template,simpleBinary,USR1:B1,USR1 B1
#% gdatag,binary,rw,USR1:B1,RECORD
#% archiver 10
#% autosave 1 VAL
record(bo, "$(dom,undefined)-EA-USER-01:B1") {
  field(DTYP, "")
  field(OUT, "")
  field(SCAN, "")
  field(HIGH, "$(HIGH,undefined)")
  field(ZNAM, "Zero")
  field(ONAM, "One")
  field(VAL, "0")
  field(DESC, "Binary Output")
  field(PINI, "NO")
}
#! Generated by VisualDCT v2.7.1-SNAPSHOT

# % macro, P, Device Prefix
# % macro, R, Device Suffix
# % macro, DTYP, Device Type
# % macro, OUT, Output link
# % macro, SCAN, Scan Rate
# % macro, HIGH, Seconds to hold signal high
# % macro, ZNAM, Name for zero state
# % macro, ONAM, Name for one state
# % macro, VAL, Initial value
# % macro, DESC, Description
# % macro, PINI, Process at startup ("YES"/"NO")
# % macro, name, Object name and gui association name
# % macro, gda_name, GDA Name
# % macro, gda_desc, GDA Description
# % macro, archiver_rate, epics archiver configuration
# This associates an edm screen with the template
# % gui, USER.B2, edmembed, bo_embed.edl, P=$(dom,undefined)-EA-USER-01:B2,R=
# % gui, USER.B2, boyembed, userIOApp_opi/bo_embed.opi, P=$(dom,undefined)-EA-USER-01:B2,R=, label=Binary Output
# This makes the component icon reflect the severity
# % gui, USER.B2, sevr
#% gdatag,template,simpleBinary,USR1:B2,USR1 B2
#% gdatag,binary,rw,USR1:B2,RECORD
#% archiver 10
#% autosave 1 VAL
record(bo, "$(dom,undefined)-EA-USER-01:B2") {
  field(DTYP, "")
  field(OUT, "")
  field(SCAN, "")
  field(HIGH, "$(HIGH,undefined)")
  field(ZNAM, "Zero")
  field(ONAM, "One")
  field(VAL, "0")
  field(DESC, "Binary Output")
  field(PINI, "NO")
}
#! Generated by VisualDCT v2.7.1-SNAPSHOT

# % macro, P, Device Prefix
# % macro, R, Device Suffix
# % macro, DTYP, Device Type
# % macro, OUT, Output link
# % macro, SCAN, Scan Rate
# % macro, HIGH, Seconds to hold signal high
# % macro, ZNAM, Name for zero state
# % macro, ONAM, Name for one state
# % macro, VAL, Initial value
# % macro, DESC, Description
# % macro, PINI, Process at startup ("YES"/"NO")
# % macro, name, Object name and gui association name
# % macro, gda_name, GDA Name
# % macro, gda_desc, GDA Description
# % macro, archiver_rate, epics archiver configuration
# This associates an edm screen with the template
# % gui, USER.B3, edmembed, bo_embed.edl, P=$(dom,undefined)-EA-USER-01:B3,R=
# % gui, USER.B3, boyembed, userIOApp_opi/bo_embed.opi, P=$(dom,undefined)-EA-USER-01:B3,R=, label=Binary Output
# This makes the component icon reflect the severity
# % gui, USER.B3, sevr
#% gdatag,template,simpleBinary,USR1:B3,USR1 B3
#% gdatag,binary,rw,USR1:B3,RECORD
#% archiver 10
#% autosave 1 VAL
record(bo, "$(dom,undefined)-EA-USER-01:B3") {
  field(DTYP, "")
  field(OUT, "")
  field(SCAN, "")
  field(HIGH, "$(HIGH,undefined)")
  field(ZNAM, "Zero")
  field(ONAM, "One")
  field(VAL, "0")
  field(DESC, "Binary Output")
  field(PINI, "NO")
}
#! Generated by VisualDCT v2.7.1-SNAPSHOT

# % macro, P, Device Prefix
# % macro, R, Device Suffix
# % macro, DTYP, Device Type
# % macro, OUT, Output link
# % macro, SCAN, Scan Rate
# % macro, HIGH, Seconds to hold signal high
# % macro, ZNAM, Name for zero state
# % macro, ONAM, Name for one state
# % macro, VAL, Initial value
# % macro, DESC, Description
# % macro, PINI, Process at startup ("YES"/"NO")
# % macro, name, Object name and gui association name
# % macro, gda_name, GDA Name
# % macro, gda_desc, GDA Description
# % macro, archiver_rate, epics archiver configuration
# This associates an edm screen with the template
# % gui, USER.B4, edmembed, bo_embed.edl, P=$(dom,undefined)-EA-USER-01:B4,R=
# % gui, USER.B4, boyembed, userIOApp_opi/bo_embed.opi, P=$(dom,undefined)-EA-USER-01:B4,R=, label=Binary Output
# This makes the component icon reflect the severity
# % gui, USER.B4, sevr
#% gdatag,template,simpleBinary,USR1:B4,USR1 B4
#% gdatag,binary,rw,USR1:B4,RECORD
#% archiver 10
#% autosave 1 VAL
record(bo, "$(dom,undefined)-EA-USER-01:B4") {
  field(DTYP, "")
  field(OUT, "")
  field(SCAN, "")
  field(HIGH, "$(HIGH,undefined)")
  field(ZNAM, "Zero")
  field(ONAM, "One")
  field(VAL, "0")
  field(DESC, "Binary Output")
  field(PINI, "NO")
}
#! Generated by VisualDCT v2.7.1-SNAPSHOT

# % macro, P, Device Prefix
# % macro, R, Device Suffix
# % macro, DTYP, Device Type
# % macro, OUT, Output link
# % macro, SCAN, Scan Rate
# % macro, HIGH, Seconds to hold signal high
# % macro, ZNAM, Name for zero state
# % macro, ONAM, Name for one state
# % macro, VAL, Initial value
# % macro, DESC, Description
# % macro, PINI, Process at startup ("YES"/"NO")
# % macro, name, Object name and gui association name
# % macro, gda_name, GDA Name
# % macro, gda_desc, GDA Description
# % macro, archiver_rate, epics archiver configuration
# This associates an edm screen with the template
# % gui, USER.B5, edmembed, bo_embed.edl, P=$(dom,undefined)-EA-USER-01:B5,R=
# % gui, USER.B5, boyembed, userIOApp_opi/bo_embed.opi, P=$(dom,undefined)-EA-USER-01:B5,R=, label=Binary Output
# This makes the component icon reflect the severity
# % gui, USER.B5, sevr
#% gdatag,template,simpleBinary,USR1:B5,USR1 B5
#% gdatag,binary,rw,USR1:B5,RECORD
#% archiver 10
#% autosave 1 VAL
record(bo, "$(dom,undefined)-EA-USER-01:B5") {
  field(DTYP, "")
  field(OUT, "")
  field(SCAN, "")
  field(HIGH, "$(HIGH,undefined)")
  field(ZNAM, "Zero")
  field(ONAM, "One")
  field(VAL, "0")
  field(DESC, "Binary Output")
  field(PINI, "NO")
}
#! Generated by VisualDCT v2.7.1-SNAPSHOT

# % macro, P, Device Prefix
# % macro, R, Device Suffix
# % macro, DTYP, Device Type
# % macro, OUT, Output link
# % macro, SCAN, Scan Rate
# % macro, HIGH, Seconds to hold signal high
# % macro, ZNAM, Name for zero state
# % macro, ONAM, Name for one state
# % macro, VAL, Initial value
# % macro, DESC, Description
# % macro, PINI, Process at startup ("YES"/"NO")
# % macro, name, Object name and gui association name
# % macro, gda_name, GDA Name
# % macro, gda_desc, GDA Description
# % macro, archiver_rate, epics archiver configuration
# This associates an edm screen with the template
# % gui, USER.B6, edmembed, bo_embed.edl, P=$(dom,undefined)-EA-USER-01:B6,R=
# % gui, USER.B6, boyembed, userIOApp_opi/bo_embed.opi, P=$(dom,undefined)-EA-USER-01:B6,R=, label=Binary Output
# This makes the component icon reflect the severity
# % gui, USER.B6, sevr
#% gdatag,template,simpleBinary,USR1:B6,USR1 B6
#% gdatag,binary,rw,USR1:B6,RECORD
#% archiver 10
#% autosave 1 VAL
record(bo, "$(dom,undefined)-EA-USER-01:B6") {
  field(DTYP, "")
  field(OUT, "")
  field(SCAN, "")
  field(HIGH, "$(HIGH,undefined)")
  field(ZNAM, "Zero")
  field(ONAM, "One")
  field(VAL, "0")
  field(DESC, "Binary Output")
  field(PINI, "NO")
}
#! Generated by VisualDCT v2.7.1-SNAPSHOT

# % macro, P, Device Prefix
# % macro, R, Device Suffix
# % macro, DTYP, Device Type
# % macro, OUT, Output link
# % macro, SCAN, Scan Rate
# % macro, HIGH, Seconds to hold signal high
# % macro, ZNAM, Name for zero state
# % macro, ONAM, Name for one state
# % macro, VAL, Initial value
# % macro, DESC, Description
# % macro, PINI, Process at startup ("YES"/"NO")
# % macro, name, Object name and gui association name
# % macro, gda_name, GDA Name
# % macro, gda_desc, GDA Description
# % macro, archiver_rate, epics archiver configuration
# This associates an edm screen with the template
# % gui, USER.B7, edmembed, bo_embed.edl, P=$(dom,undefined)-EA-USER-01:B7,R=
# % gui, USER.B7, boyembed, userIOApp_opi/bo_embed.opi, P=$(dom,undefined)-EA-USER-01:B7,R=, label=Binary Output
# This makes the component icon reflect the severity
# % gui, USER.B7, sevr
#% gdatag,template,simpleBinary,USR1:B7,USR1 B7
#% gdatag,binary,rw,USR1:B7,RECORD
#% archiver 10
#% autosave 1 VAL
record(bo, "$(dom,undefined)-EA-USER-01:B7") {
  field(DTYP, "")
  field(OUT, "")
  field(SCAN, "")
  field(HIGH, "$(HIGH,undefined)")
  field(ZNAM, "Zero")
  field(ONAM, "One")
  field(VAL, "0")
  field(DESC, "Binary Output")
  field(PINI, "NO")
}
#! Generated by VisualDCT v2.7.1-SNAPSHOT

# % macro, P, Device Prefix
# % macro, R, Device Suffix
# % macro, DTYP, Device Type
# % macro, OUT, Output link
# % macro, SCAN, Scan Rate
# % macro, HIGH, Seconds to hold signal high
# % macro, ZNAM, Name for zero state
# % macro, ONAM, Name for one state
# % macro, VAL, Initial value
# % macro, DESC, Description
# % macro, PINI, Process at startup ("YES"/"NO")
# % macro, name, Object name and gui association name
# % macro, gda_name, GDA Name
# % macro, gda_desc, GDA Description
# % macro, archiver_rate, epics archiver configuration
# This associates an edm screen with the template
# % gui, USER.B8, edmembed, bo_embed.edl, P=$(dom,undefined)-EA-USER-01:B8,R=
# % gui, USER.B8, boyembed, userIOApp_opi/bo_embed.opi, P=$(dom,undefined)-EA-USER-01:B8,R=, label=Binary Output
# This makes the component icon reflect the severity
# % gui, USER.B8, sevr
#% gdatag,template,simpleBinary,USR1:B8,USR1 B8
#% gdatag,binary,rw,USR1:B8,RECORD
#% archiver 10
#% autosave 1 VAL
record(bo, "$(dom,undefined)-EA-USER-01:B8") {
  field(DTYP, "")
  field(OUT, "")
  field(SCAN, "")
  field(HIGH, "$(HIGH,undefined)")
  field(ZNAM, "Zero")
  field(ONAM, "One")
  field(VAL, "0")
  field(DESC, "Binary Output")
  field(PINI, "NO")
}
#! Generated by VisualDCT v2.7.1-SNAPSHOT

# % macro, P, Device Prefix
# % macro, R, Device Suffix
# % macro, DTYP, Device Type
# % macro, OUT, Output link
# % macro, SCAN, Scan Rate
# % macro, HIGH, Seconds to hold signal high
# % macro, ZNAM, Name for zero state
# % macro, ONAM, Name for one state
# % macro, VAL, Initial value
# % macro, DESC, Description
# % macro, PINI, Process at startup ("YES"/"NO")
# % macro, name, Object name and gui association name
# % macro, gda_name, GDA Name
# % macro, gda_desc, GDA Description
# % macro, archiver_rate, epics archiver configuration
# This associates an edm screen with the template
# % gui, USER.B9, edmembed, bo_embed.edl, P=$(dom,undefined)-EA-USER-01:B9,R=
# % gui, USER.B9, boyembed, userIOApp_opi/bo_embed.opi, P=$(dom,undefined)-EA-USER-01:B9,R=, label=Binary Output
# This makes the component icon reflect the severity
# % gui, USER.B9, sevr
#% gdatag,template,simpleBinary,USR1:B9,USR1 B9
#% gdatag,binary,rw,USR1:B9,RECORD
#% archiver 10
#% autosave 1 VAL
record(bo, "$(dom,undefined)-EA-USER-01:B9") {
  field(DTYP, "")
  field(OUT, "")
  field(SCAN, "")
  field(HIGH, "$(HIGH,undefined)")
  field(ZNAM, "Zero")
  field(ONAM, "One")
  field(VAL, "0")
  field(DESC, "Binary Output")
  field(PINI, "NO")
}
#! Generated by VisualDCT v2.7.1-SNAPSHOT

# % macro, P, Device Prefix
# % macro, R, Device Suffix
# % macro, DTYP, Device Type
# % macro, OUT, Output link
# % macro, SCAN, Scan Rate
# % macro, HIGH, Seconds to hold signal high
# % macro, ZNAM, Name for zero state
# % macro, ONAM, Name for one state
# % macro, VAL, Initial value
# % macro, DESC, Description
# % macro, PINI, Process at startup ("YES"/"NO")
# % macro, name, Object name and gui association name
# % macro, gda_name, GDA Name
# % macro, gda_desc, GDA Description
# % macro, archiver_rate, epics archiver configuration
# This associates an edm screen with the template
# % gui, USER.B10, edmembed, bo_embed.edl, P=$(dom,undefined)-EA-USER-01:B10,R=
# % gui, USER.B10, boyembed, userIOApp_opi/bo_embed.opi, P=$(dom,undefined)-EA-USER-01:B10,R=, label=Binary Output
# This makes the component icon reflect the severity
# % gui, USER.B10, sevr
#% gdatag,template,simpleBinary,USR1:B10,USR1 B10
#% gdatag,binary,rw,USR1:B10,RECORD
#% archiver 10
#% autosave 1 VAL
record(bo, "$(dom,undefined)-EA-USER-01:B10") {
  field(DTYP, "")
  field(OUT, "")
  field(SCAN, "")
  field(HIGH, "$(HIGH,undefined)")
  field(ZNAM, "Zero")
  field(ONAM, "One")
  field(VAL, "0")
  field(DESC, "Binary Output")
  field(PINI, "NO")
}
#! Generated by VisualDCT v2.7.1-SNAPSHOT

# % macro, P, Device Prefix
# % macro, R, Device Suffix
# % macro, DTYP, Device Type
# % macro, OUT, Output link
# % macro, SCAN, Scan Rate
# % macro, HIGH, Seconds to hold signal high
# % macro, ZNAM, Name for zero state
# % macro, ONAM, Name for one state
# % macro, VAL, Initial value
# % macro, DESC, Description
# % macro, PINI, Process at startup ("YES"/"NO")
# % macro, name, Object name and gui association name
# % macro, gda_name, GDA Name
# % macro, gda_desc, GDA Description
# % macro, archiver_rate, epics archiver configuration
# This associates an edm screen with the template
# % gui, USER.B11, edmembed, bo_embed.edl, P=$(dom,undefined)-EA-USER-01:B11,R=
# % gui, USER.B11, boyembed, userIOApp_opi/bo_embed.opi, P=$(dom,undefined)-EA-USER-01:B11,R=, label=Binary Output
# This makes the component icon reflect the severity
# % gui, USER.B11, sevr
#% gdatag,template,simpleBinary,USR1:B11,USR1 B11
#% gdatag,binary,rw,USR1:B11,RECORD
#% archiver 10
#% autosave 1 VAL
record(bo, "$(dom,undefined)-EA-USER-01:B11") {
  field(DTYP, "")
  field(OUT, "")
  field(SCAN, "")
  field(HIGH, "$(HIGH,undefined)")
  field(ZNAM, "Zero")
  field(ONAM, "One")
  field(VAL, "0")
  field(DESC, "Binary Output")
  field(PINI, "NO")
}
#! Generated by VisualDCT v2.7.1-SNAPSHOT

# % macro, P, Device Prefix
# % macro, R, Device Suffix
# % macro, DTYP, Device Type
# % macro, OUT, Output link
# % macro, SCAN, Scan Rate
# % macro, HIGH, Seconds to hold signal high
# % macro, ZNAM, Name for zero state
# % macro, ONAM, Name for one state
# % macro, VAL, Initial value
# % macro, DESC, Description
# % macro, PINI, Process at startup ("YES"/"NO")
# % macro, name, Object name and gui association name
# % macro, gda_name, GDA Name
# % macro, gda_desc, GDA Description
# % macro, archiver_rate, epics archiver configuration
# This associates an edm screen with the template
# % gui, USER.B12, edmembed, bo_embed.edl, P=$(dom,undefined)-EA-USER-01:B12,R=
# % gui, USER.B12, boyembed, userIOApp_opi/bo_embed.opi, P=$(dom,undefined)-EA-USER-01:B12,R=, label=Binary Output
# This makes the component icon reflect the severity
# % gui, USER.B12, sevr
#% gdatag,template,simpleBinary,USR1:B12,USR1 B12
#% gdatag,binary,rw,USR1:B12,RECORD
#% archiver 10
#% autosave 1 VAL
record(bo, "$(dom,undefined)-EA-USER-01:B12") {
  field(DTYP, "")
  field(OUT, "")
  field(SCAN, "")
  field(HIGH, "$(HIGH,undefined)")
  field(ZNAM, "Zero")
  field(ONAM, "One")
  field(VAL, "0")
  field(DESC, "Binary Output")
  field(PINI, "NO")
}
#! Generated by VisualDCT v2.7.1-SNAPSHOT

# % macro, P, Device Prefix
# % macro, R, Device Suffix
# % macro, DTYP, Device Type
# % macro, OUT, Output link
# % macro, SCAN, Scan Rate
# % macro, HIGH, Seconds to hold signal high
# % macro, ZNAM, Name for zero state
# % macro, ONAM, Name for one state
# % macro, VAL, Initial value
# % macro, DESC, Description
# % macro, PINI, Process at startup ("YES"/"NO")
# % macro, name, Object name and gui association name
# % macro, gda_name, GDA Name
# % macro, gda_desc, GDA Description
# % macro, archiver_rate, epics archiver configuration
# This associates an edm screen with the template
# % gui, USER.B13, edmembed, bo_embed.edl, P=$(dom,undefined)-EA-USER-01:B13,R=
# % gui, USER.B13, boyembed, userIOApp_opi/bo_embed.opi, P=$(dom,undefined)-EA-USER-01:B13,R=, label=Binary Output
# This makes the component icon reflect the severity
# % gui, USER.B13, sevr
#% gdatag,template,simpleBinary,USR1:B13,USR1 B13
#% gdatag,binary,rw,USR1:B13,RECORD
#% archiver 10
#% autosave 1 VAL
record(bo, "$(dom,undefined)-EA-USER-01:B13") {
  field(DTYP, "")
  field(OUT, "")
  field(SCAN, "")
  field(HIGH, "$(HIGH,undefined)")
  field(ZNAM, "Zero")
  field(ONAM, "One")
  field(VAL, "0")
  field(DESC, "Binary Output")
  field(PINI, "NO")
}
#! Generated by VisualDCT v2.7.1-SNAPSHOT

# % macro, P, Device Prefix
# % macro, R, Device Suffix
# % macro, DTYP, Device Type
# % macro, OUT, Output link
# % macro, SCAN, Scan Rate
# % macro, HIGH, Seconds to hold signal high
# % macro, ZNAM, Name for zero state
# % macro, ONAM, Name for one state
# % macro, VAL, Initial value
# % macro, DESC, Description
# % macro, PINI, Process at startup ("YES"/"NO")
# % macro, name, Object name and gui association name
# % macro, gda_name, GDA Name
# % macro, gda_desc, GDA Description
# % macro, archiver_rate, epics archiver configuration
# This associates an edm screen with the template
# % gui, USER.B14, edmembed, bo_embed.edl, P=$(dom,undefined)-EA-USER-01:B14,R=
# % gui, USER.B14, boyembed, userIOApp_opi/bo_embed.opi, P=$(dom,undefined)-EA-USER-01:B14,R=, label=Binary Output
# This makes the component icon reflect the severity
# % gui, USER.B14, sevr
#% gdatag,template,simpleBinary,USR1:B14,USR1 B14
#% gdatag,binary,rw,USR1:B14,RECORD
#% archiver 10
#% autosave 1 VAL
record(bo, "$(dom,undefined)-EA-USER-01:B14") {
  field(DTYP, "")
  field(OUT, "")
  field(SCAN, "")
  field(HIGH, "$(HIGH,undefined)")
  field(ZNAM, "Zero")
  field(ONAM, "One")
  field(VAL, "0")
  field(DESC, "Binary Output")
  field(PINI, "NO")
}
#! Generated by VisualDCT v2.7.1-SNAPSHOT

# % macro, P, Device Prefix
# % macro, R, Device Suffix
# % macro, DTYP, Device Type
# % macro, OUT, Output link
# % macro, SCAN, Scan Rate
# % macro, HIGH, Seconds to hold signal high
# % macro, ZNAM, Name for zero state
# % macro, ONAM, Name for one state
# % macro, VAL, Initial value
# % macro, DESC, Description
# % macro, PINI, Process at startup ("YES"/"NO")
# % macro, name, Object name and gui association name
# % macro, gda_name, GDA Name
# % macro, gda_desc, GDA Description
# % macro, archiver_rate, epics archiver configuration
# This associates an edm screen with the template
# % gui, USER.B15, edmembed, bo_embed.edl, P=$(dom,undefined)-EA-USER-01:B15,R=
# % gui, USER.B15, boyembed, userIOApp_opi/bo_embed.opi, P=$(dom,undefined)-EA-USER-01:B15,R=, label=Binary Output
# This makes the component icon reflect the severity
# % gui, USER.B15, sevr
#% gdatag,template,simpleBinary,USR1:B15,USR1 B15
#% gdatag,binary,rw,USR1:B15,RECORD
#% archiver 10
#% autosave 1 VAL
record(bo, "$(dom,undefined)-EA-USER-01:B15") {
  field(DTYP, "")
  field(OUT, "")
  field(SCAN, "")
  field(HIGH, "$(HIGH,undefined)")
  field(ZNAM, "Zero")
  field(ONAM, "One")
  field(VAL, "0")
  field(DESC, "Binary Output")
  field(PINI, "NO")
}
#! Generated by VisualDCT v2.7.1-SNAPSHOT

# % macro, P, Device Prefix
# % macro, R, Device Suffix
# % macro, DTYP, Device Type
# % macro, OUT, Output link
# % macro, SCAN, Scan Rate
# % macro, HIGH, Seconds to hold signal high
# % macro, ZNAM, Name for zero state
# % macro, ONAM, Name for one state
# % macro, VAL, Initial value
# % macro, DESC, Description
# % macro, PINI, Process at startup ("YES"/"NO")
# % macro, name, Object name and gui association name
# % macro, gda_name, GDA Name
# % macro, gda_desc, GDA Description
# % macro, archiver_rate, epics archiver configuration
# This associates an edm screen with the template
# % gui, USER.B16, edmembed, bo_embed.edl, P=$(dom,undefined)-EA-USER-01:B16,R=
# % gui, USER.B16, boyembed, userIOApp_opi/bo_embed.opi, P=$(dom,undefined)-EA-USER-01:B16,R=, label=Binary Output
# This makes the component icon reflect the severity
# % gui, USER.B16, sevr
#% gdatag,template,simpleBinary,USR1:B16,USR1 B16
#% gdatag,binary,rw,USR1:B16,RECORD
#% archiver 10
#% autosave 1 VAL
record(bo, "$(dom,undefined)-EA-USER-01:B16") {
  field(DTYP, "")
  field(OUT, "")
  field(SCAN, "")
  field(HIGH, "$(HIGH,undefined)")
  field(ZNAM, "Zero")
  field(ONAM, "One")
  field(VAL, "0")
  field(DESC, "Binary Output")
  field(PINI, "NO")
}
#! Generated by VisualDCT v2.7.1-SNAPSHOT

# % macro, P, Device Prefix
# % macro, R, Device Suffix
# % macro, DTYP, Device Type
# % macro, OUT, Output link
# % macro, SCAN, Scan Rate
# % macro, PREC, Precision
# % macro, EGUL, EGU low scale
# % macro, EGUF, EGU full scale
# % macro, DRVH, Drive high limit
# % macro, DRVL, Drive low limit
# % macro, EGU, EGU
# % macro, PINI, Process on iocInit
# % macro, DESC, Description
# % macro, LINR, LINR
# % macro, name, Object name and gui association name
# % macro, gda_name, GDA Name
# % macro, gda_desc, GDA Description
# % macro, archiver_rate, epics archiver configuration
# This associates an edm screen with the template
# % gui, USER.A1, edmembed, ao_embed.edl, P=$(dom,undefined)-EA-USER-01:A1,R=
# % gui, USER.A1, boydetail, userIOApp_opi/ao_detail.opi, P=$(dom,undefined)-EA-USER-01:A1,R=, desc=Analogue Output, DESC=Analogue Output
# % gui, USER.A1, boyembed, userIOApp_opi/ao_embed_box.opi, P=$(dom,undefined)-EA-USER-01:A1,R=, desc=Analogue Output, DESC=Analogue Output
# % gui, USER.A1, boyembed, userIOApp_opi/ao_embed.opi, P=$(dom,undefined)-EA-USER-01:A1,R=, desc=Analogue Output, DESC=Analogue Output
# % gui, USER.A1, demand, Set Value, $(dom,undefined)-EA-USER-01:A1
# % gui, USER.A1, groupHeading, Settings
# % gui, USER.A1, demand, EGU, $(dom,undefined)-EA-USER-01:A1.EGU
# % gui, USER.A1, readback, EGU, $(dom,undefined)-EA-USER-01:A1.EGU
# % gui, USER.A1, demand, EGU Low, $(dom,undefined)-EA-USER-01:A1.EGUL
# % gui, USER.A1, demand, EGU Full, $(dom,undefined)-EA-USER-01:A1.EGUF
# % gui, USER.A1, demand, Drive Low, $(dom,undefined)-EA-USER-01:A1.DRVL
# % gui, USER.A1, demand, Drive High, $(dom,undefined)-EA-USER-01:A1.DRVH
# % gui, USER.A1, demand, Precision, $(dom,undefined)-EA-USER-01:A1.PREC
# % gui, USER.A1, enum, Conversion Type, $(dom,undefined)-EA-USER-01:A1.LINR
# This makes the component icon reflect the severity
# % gui, USER.A1, sevr
#% gdatag,template,simplePv,USR1:A1,USR1 A1
#% gdatag,pv,rw,USR1:A1,RECORD
#% archiver 10
#% autosave 1 VAL
record(ao, "$(dom,undefined)-EA-USER-01:A1") {
  field(DTYP, "")
  field(OUT, "")
  field(SCAN, "")
  field(LINR, "")
  field(EGUF, "")
  field(EGUL, "")
  field(DRVH, "")
  field(DRVL, "")
  field(DESC, "Analogue Output")
  field(EGU, "V")
  field(PINI, "NO")
  field(PREC, "")
}
#! Generated by VisualDCT v2.7.1-SNAPSHOT

# % macro, P, Device Prefix
# % macro, R, Device Suffix
# % macro, DTYP, Device Type
# % macro, OUT, Output link
# % macro, SCAN, Scan Rate
# % macro, PREC, Precision
# % macro, EGUL, EGU low scale
# % macro, EGUF, EGU full scale
# % macro, DRVH, Drive high limit
# % macro, DRVL, Drive low limit
# % macro, EGU, EGU
# % macro, PINI, Process on iocInit
# % macro, DESC, Description
# % macro, LINR, LINR
# % macro, name, Object name and gui association name
# % macro, gda_name, GDA Name
# % macro, gda_desc, GDA Description
# % macro, archiver_rate, epics archiver configuration
# This associates an edm screen with the template
# % gui, USER.A2, edmembed, ao_embed.edl, P=$(dom,undefined)-EA-USER-01:A2,R=
# % gui, USER.A2, boydetail, userIOApp_opi/ao_detail.opi, P=$(dom,undefined)-EA-USER-01:A2,R=, desc=Analogue Output, DESC=Analogue Output
# % gui, USER.A2, boyembed, userIOApp_opi/ao_embed_box.opi, P=$(dom,undefined)-EA-USER-01:A2,R=, desc=Analogue Output, DESC=Analogue Output
# % gui, USER.A2, boyembed, userIOApp_opi/ao_embed.opi, P=$(dom,undefined)-EA-USER-01:A2,R=, desc=Analogue Output, DESC=Analogue Output
# % gui, USER.A2, demand, Set Value, $(dom,undefined)-EA-USER-01:A2
# % gui, USER.A2, groupHeading, Settings
# % gui, USER.A2, demand, EGU, $(dom,undefined)-EA-USER-01:A2.EGU
# % gui, USER.A2, readback, EGU, $(dom,undefined)-EA-USER-01:A2.EGU
# % gui, USER.A2, demand, EGU Low, $(dom,undefined)-EA-USER-01:A2.EGUL
# % gui, USER.A2, demand, EGU Full, $(dom,undefined)-EA-USER-01:A2.EGUF
# % gui, USER.A2, demand, Drive Low, $(dom,undefined)-EA-USER-01:A2.DRVL
# % gui, USER.A2, demand, Drive High, $(dom,undefined)-EA-USER-01:A2.DRVH
# % gui, USER.A2, demand, Precision, $(dom,undefined)-EA-USER-01:A2.PREC
# % gui, USER.A2, enum, Conversion Type, $(dom,undefined)-EA-USER-01:A2.LINR
# This makes the component icon reflect the severity
# % gui, USER.A2, sevr
#% gdatag,template,simplePv,USR1:A2,USR1 A2
#% gdatag,pv,rw,USR1:A2,RECORD
#% archiver 10
#% autosave 1 VAL
record(ao, "$(dom,undefined)-EA-USER-01:A2") {
  field(DTYP, "")
  field(OUT, "")
  field(SCAN, "")
  field(LINR, "")
  field(EGUF, "")
  field(EGUL, "")
  field(DRVH, "")
  field(DRVL, "")
  field(DESC, "Analogue Output")
  field(EGU, "V")
  field(PINI, "NO")
  field(PREC, "")
}
#! Generated by VisualDCT v2.7.1-SNAPSHOT

# % macro, P, Device Prefix
# % macro, R, Device Suffix
# % macro, DTYP, Device Type
# % macro, OUT, Output link
# % macro, SCAN, Scan Rate
# % macro, PREC, Precision
# % macro, EGUL, EGU low scale
# % macro, EGUF, EGU full scale
# % macro, DRVH, Drive high limit
# % macro, DRVL, Drive low limit
# % macro, EGU, EGU
# % macro, PINI, Process on iocInit
# % macro, DESC, Description
# % macro, LINR, LINR
# % macro, name, Object name and gui association name
# % macro, gda_name, GDA Name
# % macro, gda_desc, GDA Description
# % macro, archiver_rate, epics archiver configuration
# This associates an edm screen with the template
# % gui, USER.A3, edmembed, ao_embed.edl, P=$(dom,undefined)-EA-USER-01:A3,R=
# % gui, USER.A3, boydetail, userIOApp_opi/ao_detail.opi, P=$(dom,undefined)-EA-USER-01:A3,R=, desc=Analogue Output, DESC=Analogue Output
# % gui, USER.A3, boyembed, userIOApp_opi/ao_embed_box.opi, P=$(dom,undefined)-EA-USER-01:A3,R=, desc=Analogue Output, DESC=Analogue Output
# % gui, USER.A3, boyembed, userIOApp_opi/ao_embed.opi, P=$(dom,undefined)-EA-USER-01:A3,R=, desc=Analogue Output, DESC=Analogue Output
# % gui, USER.A3, demand, Set Value, $(dom,undefined)-EA-USER-01:A3
# % gui, USER.A3, groupHeading, Settings
# % gui, USER.A3, demand, EGU, $(dom,undefined)-EA-USER-01:A3.EGU
# % gui, USER.A3, readback, EGU, $(dom,undefined)-EA-USER-01:A3.EGU
# % gui, USER.A3, demand, EGU Low, $(dom,undefined)-EA-USER-01:A3.EGUL
# % gui, USER.A3, demand, EGU Full, $(dom,undefined)-EA-USER-01:A3.EGUF
# % gui, USER.A3, demand, Drive Low, $(dom,undefined)-EA-USER-01:A3.DRVL
# % gui, USER.A3, demand, Drive High, $(dom,undefined)-EA-USER-01:A3.DRVH
# % gui, USER.A3, demand, Precision, $(dom,undefined)-EA-USER-01:A3.PREC
# % gui, USER.A3, enum, Conversion Type, $(dom,undefined)-EA-USER-01:A3.LINR
# This makes the component icon reflect the severity
# % gui, USER.A3, sevr
#% gdatag,template,simplePv,USR1:A3,USR1 A3
#% gdatag,pv,rw,USR1:A3,RECORD
#% archiver 10
#% autosave 1 VAL
record(ao, "$(dom,undefined)-EA-USER-01:A3") {
  field(DTYP, "")
  field(OUT, "")
  field(SCAN, "")
  field(LINR, "")
  field(EGUF, "")
  field(EGUL, "")
  field(DRVH, "")
  field(DRVL, "")
  field(DESC, "Analogue Output")
  field(EGU, "V")
  field(PINI, "NO")
  field(PREC, "")
}
#! Generated by VisualDCT v2.7.1-SNAPSHOT

# % macro, P, Device Prefix
# % macro, R, Device Suffix
# % macro, DTYP, Device Type
# % macro, OUT, Output link
# % macro, SCAN, Scan Rate
# % macro, PREC, Precision
# % macro, EGUL, EGU low scale
# % macro, EGUF, EGU full scale
# % macro, DRVH, Drive high limit
# % macro, DRVL, Drive low limit
# % macro, EGU, EGU
# % macro, PINI, Process on iocInit
# % macro, DESC, Description
# % macro, LINR, LINR
# % macro, name, Object name and gui association name
# % macro, gda_name, GDA Name
# % macro, gda_desc, GDA Description
# % macro, archiver_rate, epics archiver configuration
# This associates an edm screen with the template
# % gui, USER.A4, edmembed, ao_embed.edl, P=$(dom,undefined)-EA-USER-01:A4,R=
# % gui, USER.A4, boydetail, userIOApp_opi/ao_detail.opi, P=$(dom,undefined)-EA-USER-01:A4,R=, desc=Analogue Output, DESC=Analogue Output
# % gui, USER.A4, boyembed, userIOApp_opi/ao_embed_box.opi, P=$(dom,undefined)-EA-USER-01:A4,R=, desc=Analogue Output, DESC=Analogue Output
# % gui, USER.A4, boyembed, userIOApp_opi/ao_embed.opi, P=$(dom,undefined)-EA-USER-01:A4,R=, desc=Analogue Output, DESC=Analogue Output
# % gui, USER.A4, demand, Set Value, $(dom,undefined)-EA-USER-01:A4
# % gui, USER.A4, groupHeading, Settings
# % gui, USER.A4, demand, EGU, $(dom,undefined)-EA-USER-01:A4.EGU
# % gui, USER.A4, readback, EGU, $(dom,undefined)-EA-USER-01:A4.EGU
# % gui, USER.A4, demand, EGU Low, $(dom,undefined)-EA-USER-01:A4.EGUL
# % gui, USER.A4, demand, EGU Full, $(dom,undefined)-EA-USER-01:A4.EGUF
# % gui, USER.A4, demand, Drive Low, $(dom,undefined)-EA-USER-01:A4.DRVL
# % gui, USER.A4, demand, Drive High, $(dom,undefined)-EA-USER-01:A4.DRVH
# % gui, USER.A4, demand, Precision, $(dom,undefined)-EA-USER-01:A4.PREC
# % gui, USER.A4, enum, Conversion Type, $(dom,undefined)-EA-USER-01:A4.LINR
# This makes the component icon reflect the severity
# % gui, USER.A4, sevr
#% gdatag,template,simplePv,USR1:A4,USR1 A4
#% gdatag,pv,rw,USR1:A4,RECORD
#% archiver 10
#% autosave 1 VAL
record(ao, "$(dom,undefined)-EA-USER-01:A4") {
  field(DTYP, "")
  field(OUT, "")
  field(SCAN, "")
  field(LINR, "")
  field(EGUF, "")
  field(EGUL, "")
  field(DRVH, "")
  field(DRVL, "")
  field(DESC, "Analogue Output")
  field(EGU, "V")
  field(PINI, "NO")
  field(PREC, "")
}
#! Generated by VisualDCT v2.7.1-SNAPSHOT

# % macro, P, Device Prefix
# % macro, R, Device Suffix
# % macro, DTYP, Device Type
# % macro, OUT, Output link
# % macro, SCAN, Scan Rate
# % macro, PREC, Precision
# % macro, EGUL, EGU low scale
# % macro, EGUF, EGU full scale
# % macro, DRVH, Drive high limit
# % macro, DRVL, Drive low limit
# % macro, EGU, EGU
# % macro, PINI, Process on iocInit
# % macro, DESC, Description
# % macro, LINR, LINR
# % macro, name, Object name and gui association name
# % macro, gda_name, GDA Name
# % macro, gda_desc, GDA Description
# % macro, archiver_rate, epics archiver configuration
# This associates an edm screen with the template
# % gui, USER.A5, edmembed, ao_embed.edl, P=$(dom,undefined)-EA-USER-01:A5,R=
# % gui, USER.A5, boydetail, userIOApp_opi/ao_detail.opi, P=$(dom,undefined)-EA-USER-01:A5,R=, desc=Analogue Output, DESC=Analogue Output
# % gui, USER.A5, boyembed, userIOApp_opi/ao_embed_box.opi, P=$(dom,undefined)-EA-USER-01:A5,R=, desc=Analogue Output, DESC=Analogue Output
# % gui, USER.A5, boyembed, userIOApp_opi/ao_embed.opi, P=$(dom,undefined)-EA-USER-01:A5,R=, desc=Analogue Output, DESC=Analogue Output
# % gui, USER.A5, demand, Set Value, $(dom,undefined)-EA-USER-01:A5
# % gui, USER.A5, groupHeading, Settings
# % gui, USER.A5, demand, EGU, $(dom,undefined)-EA-USER-01:A5.EGU
# % gui, USER.A5, readback, EGU, $(dom,undefined)-EA-USER-01:A5.EGU
# % gui, USER.A5, demand, EGU Low, $(dom,undefined)-EA-USER-01:A5.EGUL
# % gui, USER.A5, demand, EGU Full, $(dom,undefined)-EA-USER-01:A5.EGUF
# % gui, USER.A5, demand, Drive Low, $(dom,undefined)-EA-USER-01:A5.DRVL
# % gui, USER.A5, demand, Drive High, $(dom,undefined)-EA-USER-01:A5.DRVH
# % gui, USER.A5, demand, Precision, $(dom,undefined)-EA-USER-01:A5.PREC
# % gui, USER.A5, enum, Conversion Type, $(dom,undefined)-EA-USER-01:A5.LINR
# This makes the component icon reflect the severity
# % gui, USER.A5, sevr
#% gdatag,template,simplePv,USR1:A5,USR1 A5
#% gdatag,pv,rw,USR1:A5,RECORD
#% archiver 10
#% autosave 1 VAL
record(ao, "$(dom,undefined)-EA-USER-01:A5") {
  field(DTYP, "")
  field(OUT, "")
  field(SCAN, "")
  field(LINR, "")
  field(EGUF, "")
  field(EGUL, "")
  field(DRVH, "")
  field(DRVL, "")
  field(DESC, "Analogue Output")
  field(EGU, "V")
  field(PINI, "NO")
  field(PREC, "")
}
#! Generated by VisualDCT v2.7.1-SNAPSHOT

# % macro, P, Device Prefix
# % macro, R, Device Suffix
# % macro, DTYP, Device Type
# % macro, OUT, Output link
# % macro, SCAN, Scan Rate
# % macro, PREC, Precision
# % macro, EGUL, EGU low scale
# % macro, EGUF, EGU full scale
# % macro, DRVH, Drive high limit
# % macro, DRVL, Drive low limit
# % macro, EGU, EGU
# % macro, PINI, Process on iocInit
# % macro, DESC, Description
# % macro, LINR, LINR
# % macro, name, Object name and gui association name
# % macro, gda_name, GDA Name
# % macro, gda_desc, GDA Description
# % macro, archiver_rate, epics archiver configuration
# This associates an edm screen with the template
# % gui, USER.A6, edmembed, ao_embed.edl, P=$(dom,undefined)-EA-USER-01:A6,R=
# % gui, USER.A6, boydetail, userIOApp_opi/ao_detail.opi, P=$(dom,undefined)-EA-USER-01:A6,R=, desc=Analogue Output, DESC=Analogue Output
# % gui, USER.A6, boyembed, userIOApp_opi/ao_embed_box.opi, P=$(dom,undefined)-EA-USER-01:A6,R=, desc=Analogue Output, DESC=Analogue Output
# % gui, USER.A6, boyembed, userIOApp_opi/ao_embed.opi, P=$(dom,undefined)-EA-USER-01:A6,R=, desc=Analogue Output, DESC=Analogue Output
# % gui, USER.A6, demand, Set Value, $(dom,undefined)-EA-USER-01:A6
# % gui, USER.A6, groupHeading, Settings
# % gui, USER.A6, demand, EGU, $(dom,undefined)-EA-USER-01:A6.EGU
# % gui, USER.A6, readback, EGU, $(dom,undefined)-EA-USER-01:A6.EGU
# % gui, USER.A6, demand, EGU Low, $(dom,undefined)-EA-USER-01:A6.EGUL
# % gui, USER.A6, demand, EGU Full, $(dom,undefined)-EA-USER-01:A6.EGUF
# % gui, USER.A6, demand, Drive Low, $(dom,undefined)-EA-USER-01:A6.DRVL
# % gui, USER.A6, demand, Drive High, $(dom,undefined)-EA-USER-01:A6.DRVH
# % gui, USER.A6, demand, Precision, $(dom,undefined)-EA-USER-01:A6.PREC
# % gui, USER.A6, enum, Conversion Type, $(dom,undefined)-EA-USER-01:A6.LINR
# This makes the component icon reflect the severity
# % gui, USER.A6, sevr
#% gdatag,template,simplePv,USR1:A6,USR1 A6
#% gdatag,pv,rw,USR1:A6,RECORD
#% archiver 10
#% autosave 1 VAL
record(ao, "$(dom,undefined)-EA-USER-01:A6") {
  field(DTYP, "")
  field(OUT, "")
  field(SCAN, "")
  field(LINR, "")
  field(EGUF, "")
  field(EGUL, "")
  field(DRVH, "")
  field(DRVL, "")
  field(DESC, "Analogue Output")
  field(EGU, "V")
  field(PINI, "NO")
  field(PREC, "")
}
#! Generated by VisualDCT v2.7.1-SNAPSHOT

# % macro, P, Device Prefix
# % macro, R, Device Suffix
# % macro, DTYP, Device Type
# % macro, OUT, Output link
# % macro, SCAN, Scan Rate
# % macro, PREC, Precision
# % macro, EGUL, EGU low scale
# % macro, EGUF, EGU full scale
# % macro, DRVH, Drive high limit
# % macro, DRVL, Drive low limit
# % macro, EGU, EGU
# % macro, PINI, Process on iocInit
# % macro, DESC, Description
# % macro, LINR, LINR
# % macro, name, Object name and gui association name
# % macro, gda_name, GDA Name
# % macro, gda_desc, GDA Description
# % macro, archiver_rate, epics archiver configuration
# This associates an edm screen with the template
# % gui, USER.A7, edmembed, ao_embed.edl, P=$(dom,undefined)-EA-USER-01:A7,R=
# % gui, USER.A7, boydetail, userIOApp_opi/ao_detail.opi, P=$(dom,undefined)-EA-USER-01:A7,R=, desc=Analogue Output, DESC=Analogue Output
# % gui, USER.A7, boyembed, userIOApp_opi/ao_embed_box.opi, P=$(dom,undefined)-EA-USER-01:A7,R=, desc=Analogue Output, DESC=Analogue Output
# % gui, USER.A7, boyembed, userIOApp_opi/ao_embed.opi, P=$(dom,undefined)-EA-USER-01:A7,R=, desc=Analogue Output, DESC=Analogue Output
# % gui, USER.A7, demand, Set Value, $(dom,undefined)-EA-USER-01:A7
# % gui, USER.A7, groupHeading, Settings
# % gui, USER.A7, demand, EGU, $(dom,undefined)-EA-USER-01:A7.EGU
# % gui, USER.A7, readback, EGU, $(dom,undefined)-EA-USER-01:A7.EGU
# % gui, USER.A7, demand, EGU Low, $(dom,undefined)-EA-USER-01:A7.EGUL
# % gui, USER.A7, demand, EGU Full, $(dom,undefined)-EA-USER-01:A7.EGUF
# % gui, USER.A7, demand, Drive Low, $(dom,undefined)-EA-USER-01:A7.DRVL
# % gui, USER.A7, demand, Drive High, $(dom,undefined)-EA-USER-01:A7.DRVH
# % gui, USER.A7, demand, Precision, $(dom,undefined)-EA-USER-01:A7.PREC
# % gui, USER.A7, enum, Conversion Type, $(dom,undefined)-EA-USER-01:A7.LINR
# This makes the component icon reflect the severity
# % gui, USER.A7, sevr
#% gdatag,template,simplePv,USR1:A7,USR1 A7
#% gdatag,pv,rw,USR1:A7,RECORD
#% archiver 10
#% autosave 1 VAL
record(ao, "$(dom,undefined)-EA-USER-01:A7") {
  field(DTYP, "")
  field(OUT, "")
  field(SCAN, "")
  field(LINR, "")
  field(EGUF, "")
  field(EGUL, "")
  field(DRVH, "")
  field(DRVL, "")
  field(DESC, "Analogue Output")
  field(EGU, "V")
  field(PINI, "NO")
  field(PREC, "")
}
#! Generated by VisualDCT v2.7.1-SNAPSHOT

# % macro, P, Device Prefix
# % macro, R, Device Suffix
# % macro, DTYP, Device Type
# % macro, OUT, Output link
# % macro, SCAN, Scan Rate
# % macro, PREC, Precision
# % macro, EGUL, EGU low scale
# % macro, EGUF, EGU full scale
# % macro, DRVH, Drive high limit
# % macro, DRVL, Drive low limit
# % macro, EGU, EGU
# % macro, PINI, Process on iocInit
# % macro, DESC, Description
# % macro, LINR, LINR
# % macro, name, Object name and gui association name
# % macro, gda_name, GDA Name
# % macro, gda_desc, GDA Description
# % macro, archiver_rate, epics archiver configuration
# This associates an edm screen with the template
# % gui, USER.A8, edmembed, ao_embed.edl, P=$(dom,undefined)-EA-USER-01:A8,R=
# % gui, USER.A8, boydetail, userIOApp_opi/ao_detail.opi, P=$(dom,undefined)-EA-USER-01:A8,R=, desc=Analogue Output, DESC=Analogue Output
# % gui, USER.A8, boyembed, userIOApp_opi/ao_embed_box.opi, P=$(dom,undefined)-EA-USER-01:A8,R=, desc=Analogue Output, DESC=Analogue Output
# % gui, USER.A8, boyembed, userIOApp_opi/ao_embed.opi, P=$(dom,undefined)-EA-USER-01:A8,R=, desc=Analogue Output, DESC=Analogue Output
# % gui, USER.A8, demand, Set Value, $(dom,undefined)-EA-USER-01:A8
# % gui, USER.A8, groupHeading, Settings
# % gui, USER.A8, demand, EGU, $(dom,undefined)-EA-USER-01:A8.EGU
# % gui, USER.A8, readback, EGU, $(dom,undefined)-EA-USER-01:A8.EGU
# % gui, USER.A8, demand, EGU Low, $(dom,undefined)-EA-USER-01:A8.EGUL
# % gui, USER.A8, demand, EGU Full, $(dom,undefined)-EA-USER-01:A8.EGUF
# % gui, USER.A8, demand, Drive Low, $(dom,undefined)-EA-USER-01:A8.DRVL
# % gui, USER.A8, demand, Drive High, $(dom,undefined)-EA-USER-01:A8.DRVH
# % gui, USER.A8, demand, Precision, $(dom,undefined)-EA-USER-01:A8.PREC
# % gui, USER.A8, enum, Conversion Type, $(dom,undefined)-EA-USER-01:A8.LINR
# This makes the component icon reflect the severity
# % gui, USER.A8, sevr
#% gdatag,template,simplePv,USR1:A8,USR1 A8
#% gdatag,pv,rw,USR1:A8,RECORD
#% archiver 10
#% autosave 1 VAL
record(ao, "$(dom,undefined)-EA-USER-01:A8") {
  field(DTYP, "")
  field(OUT, "")
  field(SCAN, "")
  field(LINR, "")
  field(EGUF, "")
  field(EGUL, "")
  field(DRVH, "")
  field(DRVL, "")
  field(DESC, "Analogue Output")
  field(EGU, "V")
  field(PINI, "NO")
  field(PREC, "")
}
#! Generated by VisualDCT v2.7.1-SNAPSHOT

# % macro, P, Device Prefix
# % macro, R, Device Suffix
# % macro, DTYP, Device Type
# % macro, OUT, Output link
# % macro, SCAN, Scan Rate
# % macro, PREC, Precision
# % macro, EGUL, EGU low scale
# % macro, EGUF, EGU full scale
# % macro, DRVH, Drive high limit
# % macro, DRVL, Drive low limit
# % macro, EGU, EGU
# % macro, PINI, Process on iocInit
# % macro, DESC, Description
# % macro, LINR, LINR
# % macro, name, Object name and gui association name
# % macro, gda_name, GDA Name
# % macro, gda_desc, GDA Description
# % macro, archiver_rate, epics archiver configuration
# This associates an edm screen with the template
# % gui, USER.A9, edmembed, ao_embed.edl, P=$(dom,undefined)-EA-USER-01:A9,R=
# % gui, USER.A9, boydetail, userIOApp_opi/ao_detail.opi, P=$(dom,undefined)-EA-USER-01:A9,R=, desc=Analogue Output, DESC=Analogue Output
# % gui, USER.A9, boyembed, userIOApp_opi/ao_embed_box.opi, P=$(dom,undefined)-EA-USER-01:A9,R=, desc=Analogue Output, DESC=Analogue Output
# % gui, USER.A9, boyembed, userIOApp_opi/ao_embed.opi, P=$(dom,undefined)-EA-USER-01:A9,R=, desc=Analogue Output, DESC=Analogue Output
# % gui, USER.A9, demand, Set Value, $(dom,undefined)-EA-USER-01:A9
# % gui, USER.A9, groupHeading, Settings
# % gui, USER.A9, demand, EGU, $(dom,undefined)-EA-USER-01:A9.EGU
# % gui, USER.A9, readback, EGU, $(dom,undefined)-EA-USER-01:A9.EGU
# % gui, USER.A9, demand, EGU Low, $(dom,undefined)-EA-USER-01:A9.EGUL
# % gui, USER.A9, demand, EGU Full, $(dom,undefined)-EA-USER-01:A9.EGUF
# % gui, USER.A9, demand, Drive Low, $(dom,undefined)-EA-USER-01:A9.DRVL
# % gui, USER.A9, demand, Drive High, $(dom,undefined)-EA-USER-01:A9.DRVH
# % gui, USER.A9, demand, Precision, $(dom,undefined)-EA-USER-01:A9.PREC
# % gui, USER.A9, enum, Conversion Type, $(dom,undefined)-EA-USER-01:A9.LINR
# This makes the component icon reflect the severity
# % gui, USER.A9, sevr
#% gdatag,template,simplePv,USR1:A9,USR1 A9
#% gdatag,pv,rw,USR1:A9,RECORD
#% archiver 10
#% autosave 1 VAL
record(ao, "$(dom,undefined)-EA-USER-01:A9") {
  field(DTYP, "")
  field(OUT, "")
  field(SCAN, "")
  field(LINR, "")
  field(EGUF, "")
  field(EGUL, "")
  field(DRVH, "")
  field(DRVL, "")
  field(DESC, "Analogue Output")
  field(EGU, "V")
  field(PINI, "NO")
  field(PREC, "")
}
#! Generated by VisualDCT v2.7.1-SNAPSHOT

# % macro, P, Device Prefix
# % macro, R, Device Suffix
# % macro, DTYP, Device Type
# % macro, OUT, Output link
# % macro, SCAN, Scan Rate
# % macro, PREC, Precision
# % macro, EGUL, EGU low scale
# % macro, EGUF, EGU full scale
# % macro, DRVH, Drive high limit
# % macro, DRVL, Drive low limit
# % macro, EGU, EGU
# % macro, PINI, Process on iocInit
# % macro, DESC, Description
# % macro, LINR, LINR
# % macro, name, Object name and gui association name
# % macro, gda_name, GDA Name
# % macro, gda_desc, GDA Description
# % macro, archiver_rate, epics archiver configuration
# This associates an edm screen with the template
# % gui, USER.A10, edmembed, ao_embed.edl, P=$(dom,undefined)-EA-USER-01:A10,R=
# % gui, USER.A10, boydetail, userIOApp_opi/ao_detail.opi, P=$(dom,undefined)-EA-USER-01:A10,R=, desc=Analogue Output, DESC=Analogue Output
# % gui, USER.A10, boyembed, userIOApp_opi/ao_embed_box.opi, P=$(dom,undefined)-EA-USER-01:A10,R=, desc=Analogue Output, DESC=Analogue Output
# % gui, USER.A10, boyembed, userIOApp_opi/ao_embed.opi, P=$(dom,undefined)-EA-USER-01:A10,R=, desc=Analogue Output, DESC=Analogue Output
# % gui, USER.A10, demand, Set Value, $(dom,undefined)-EA-USER-01:A10
# % gui, USER.A10, groupHeading, Settings
# % gui, USER.A10, demand, EGU, $(dom,undefined)-EA-USER-01:A10.EGU
# % gui, USER.A10, readback, EGU, $(dom,undefined)-EA-USER-01:A10.EGU
# % gui, USER.A10, demand, EGU Low, $(dom,undefined)-EA-USER-01:A10.EGUL
# % gui, USER.A10, demand, EGU Full, $(dom,undefined)-EA-USER-01:A10.EGUF
# % gui, USER.A10, demand, Drive Low, $(dom,undefined)-EA-USER-01:A10.DRVL
# % gui, USER.A10, demand, Drive High, $(dom,undefined)-EA-USER-01:A10.DRVH
# % gui, USER.A10, demand, Precision, $(dom,undefined)-EA-USER-01:A10.PREC
# % gui, USER.A10, enum, Conversion Type, $(dom,undefined)-EA-USER-01:A10.LINR
# This makes the component icon reflect the severity
# % gui, USER.A10, sevr
#% gdatag,template,simplePv,USR1:A10,USR1 A10
#% gdatag,pv,rw,USR1:A10,RECORD
#% archiver 10
#% autosave 1 VAL
record(ao, "$(dom,undefined)-EA-USER-01:A10") {
  field(DTYP, "")
  field(OUT, "")
  field(SCAN, "")
  field(LINR, "")
  field(EGUF, "")
  field(EGUL, "")
  field(DRVH, "")
  field(DRVL, "")
  field(DESC, "Analogue Output")
  field(EGU, "V")
  field(PINI, "NO")
  field(PREC, "")
}
#! Generated by VisualDCT v2.7.1-SNAPSHOT

# % macro, P, Device Prefix
# % macro, R, Device Suffix
# % macro, DTYP, Device Type
# % macro, OUT, Output link
# % macro, SCAN, Scan Rate
# % macro, PREC, Precision
# % macro, EGUL, EGU low scale
# % macro, EGUF, EGU full scale
# % macro, DRVH, Drive high limit
# % macro, DRVL, Drive low limit
# % macro, EGU, EGU
# % macro, PINI, Process on iocInit
# % macro, DESC, Description
# % macro, LINR, LINR
# % macro, name, Object name and gui association name
# % macro, gda_name, GDA Name
# % macro, gda_desc, GDA Description
# % macro, archiver_rate, epics archiver configuration
# This associates an edm screen with the template
# % gui, USER.A11, edmembed, ao_embed.edl, P=$(dom,undefined)-EA-USER-01:A11,R=
# % gui, USER.A11, boydetail, userIOApp_opi/ao_detail.opi, P=$(dom,undefined)-EA-USER-01:A11,R=, desc=Analogue Output, DESC=Analogue Output
# % gui, USER.A11, boyembed, userIOApp_opi/ao_embed_box.opi, P=$(dom,undefined)-EA-USER-01:A11,R=, desc=Analogue Output, DESC=Analogue Output
# % gui, USER.A11, boyembed, userIOApp_opi/ao_embed.opi, P=$(dom,undefined)-EA-USER-01:A11,R=, desc=Analogue Output, DESC=Analogue Output
# % gui, USER.A11, demand, Set Value, $(dom,undefined)-EA-USER-01:A11
# % gui, USER.A11, groupHeading, Settings
# % gui, USER.A11, demand, EGU, $(dom,undefined)-EA-USER-01:A11.EGU
# % gui, USER.A11, readback, EGU, $(dom,undefined)-EA-USER-01:A11.EGU
# % gui, USER.A11, demand, EGU Low, $(dom,undefined)-EA-USER-01:A11.EGUL
# % gui, USER.A11, demand, EGU Full, $(dom,undefined)-EA-USER-01:A11.EGUF
# % gui, USER.A11, demand, Drive Low, $(dom,undefined)-EA-USER-01:A11.DRVL
# % gui, USER.A11, demand, Drive High, $(dom,undefined)-EA-USER-01:A11.DRVH
# % gui, USER.A11, demand, Precision, $(dom,undefined)-EA-USER-01:A11.PREC
# % gui, USER.A11, enum, Conversion Type, $(dom,undefined)-EA-USER-01:A11.LINR
# This makes the component icon reflect the severity
# % gui, USER.A11, sevr
#% gdatag,template,simplePv,USR1:A11,USR1 A11
#% gdatag,pv,rw,USR1:A11,RECORD
#% archiver 10
#% autosave 1 VAL
record(ao, "$(dom,undefined)-EA-USER-01:A11") {
  field(DTYP, "")
  field(OUT, "")
  field(SCAN, "")
  field(LINR, "")
  field(EGUF, "")
  field(EGUL, "")
  field(DRVH, "")
  field(DRVL, "")
  field(DESC, "Analogue Output")
  field(EGU, "V")
  field(PINI, "NO")
  field(PREC, "")
}
#! Generated by VisualDCT v2.7.1-SNAPSHOT

# % macro, P, Device Prefix
# % macro, R, Device Suffix
# % macro, DTYP, Device Type
# % macro, OUT, Output link
# % macro, SCAN, Scan Rate
# % macro, PREC, Precision
# % macro, EGUL, EGU low scale
# % macro, EGUF, EGU full scale
# % macro, DRVH, Drive high limit
# % macro, DRVL, Drive low limit
# % macro, EGU, EGU
# % macro, PINI, Process on iocInit
# % macro, DESC, Description
# % macro, LINR, LINR
# % macro, name, Object name and gui association name
# % macro, gda_name, GDA Name
# % macro, gda_desc, GDA Description
# % macro, archiver_rate, epics archiver configuration
# This associates an edm screen with the template
# % gui, USER.A12, edmembed, ao_embed.edl, P=$(dom,undefined)-EA-USER-01:A12,R=
# % gui, USER.A12, boydetail, userIOApp_opi/ao_detail.opi, P=$(dom,undefined)-EA-USER-01:A12,R=, desc=Analogue Output, DESC=Analogue Output
# % gui, USER.A12, boyembed, userIOApp_opi/ao_embed_box.opi, P=$(dom,undefined)-EA-USER-01:A12,R=, desc=Analogue Output, DESC=Analogue Output
# % gui, USER.A12, boyembed, userIOApp_opi/ao_embed.opi, P=$(dom,undefined)-EA-USER-01:A12,R=, desc=Analogue Output, DESC=Analogue Output
# % gui, USER.A12, demand, Set Value, $(dom,undefined)-EA-USER-01:A12
# % gui, USER.A12, groupHeading, Settings
# % gui, USER.A12, demand, EGU, $(dom,undefined)-EA-USER-01:A12.EGU
# % gui, USER.A12, readback, EGU, $(dom,undefined)-EA-USER-01:A12.EGU
# % gui, USER.A12, demand, EGU Low, $(dom,undefined)-EA-USER-01:A12.EGUL
# % gui, USER.A12, demand, EGU Full, $(dom,undefined)-EA-USER-01:A12.EGUF
# % gui, USER.A12, demand, Drive Low, $(dom,undefined)-EA-USER-01:A12.DRVL
# % gui, USER.A12, demand, Drive High, $(dom,undefined)-EA-USER-01:A12.DRVH
# % gui, USER.A12, demand, Precision, $(dom,undefined)-EA-USER-01:A12.PREC
# % gui, USER.A12, enum, Conversion Type, $(dom,undefined)-EA-USER-01:A12.LINR
# This makes the component icon reflect the severity
# % gui, USER.A12, sevr
#% gdatag,template,simplePv,USR1:A12,USR1 A12
#% gdatag,pv,rw,USR1:A12,RECORD
#% archiver 10
#% autosave 1 VAL
record(ao, "$(dom,undefined)-EA-USER-01:A12") {
  field(DTYP, "")
  field(OUT, "")
  field(SCAN, "")
  field(LINR, "")
  field(EGUF, "")
  field(EGUL, "")
  field(DRVH, "")
  field(DRVL, "")
  field(DESC, "Analogue Output")
  field(EGU, "V")
  field(PINI, "NO")
  field(PREC, "")
}
#! Generated by VisualDCT v2.7.1-SNAPSHOT

# % macro, P, Device Prefix
# % macro, R, Device Suffix
# % macro, DTYP, Device Type
# % macro, OUT, Output link
# % macro, SCAN, Scan Rate
# % macro, PREC, Precision
# % macro, EGUL, EGU low scale
# % macro, EGUF, EGU full scale
# % macro, DRVH, Drive high limit
# % macro, DRVL, Drive low limit
# % macro, EGU, EGU
# % macro, PINI, Process on iocInit
# % macro, DESC, Description
# % macro, LINR, LINR
# % macro, name, Object name and gui association name
# % macro, gda_name, GDA Name
# % macro, gda_desc, GDA Description
# % macro, archiver_rate, epics archiver configuration
# This associates an edm screen with the template
# % gui, USER.A13, edmembed, ao_embed.edl, P=$(dom,undefined)-EA-USER-01:A13,R=
# % gui, USER.A13, boydetail, userIOApp_opi/ao_detail.opi, P=$(dom,undefined)-EA-USER-01:A13,R=, desc=Analogue Output, DESC=Analogue Output
# % gui, USER.A13, boyembed, userIOApp_opi/ao_embed_box.opi, P=$(dom,undefined)-EA-USER-01:A13,R=, desc=Analogue Output, DESC=Analogue Output
# % gui, USER.A13, boyembed, userIOApp_opi/ao_embed.opi, P=$(dom,undefined)-EA-USER-01:A13,R=, desc=Analogue Output, DESC=Analogue Output
# % gui, USER.A13, demand, Set Value, $(dom,undefined)-EA-USER-01:A13
# % gui, USER.A13, groupHeading, Settings
# % gui, USER.A13, demand, EGU, $(dom,undefined)-EA-USER-01:A13.EGU
# % gui, USER.A13, readback, EGU, $(dom,undefined)-EA-USER-01:A13.EGU
# % gui, USER.A13, demand, EGU Low, $(dom,undefined)-EA-USER-01:A13.EGUL
# % gui, USER.A13, demand, EGU Full, $(dom,undefined)-EA-USER-01:A13.EGUF
# % gui, USER.A13, demand, Drive Low, $(dom,undefined)-EA-USER-01:A13.DRVL
# % gui, USER.A13, demand, Drive High, $(dom,undefined)-EA-USER-01:A13.DRVH
# % gui, USER.A13, demand, Precision, $(dom,undefined)-EA-USER-01:A13.PREC
# % gui, USER.A13, enum, Conversion Type, $(dom,undefined)-EA-USER-01:A13.LINR
# This makes the component icon reflect the severity
# % gui, USER.A13, sevr
#% gdatag,template,simplePv,USR1:A13,USR1 A13
#% gdatag,pv,rw,USR1:A13,RECORD
#% archiver 10
#% autosave 1 VAL
record(ao, "$(dom,undefined)-EA-USER-01:A13") {
  field(DTYP, "")
  field(OUT, "")
  field(SCAN, "")
  field(LINR, "")
  field(EGUF, "")
  field(EGUL, "")
  field(DRVH, "")
  field(DRVL, "")
  field(DESC, "Analogue Output")
  field(EGU, "V")
  field(PINI, "NO")
  field(PREC, "")
}
#! Generated by VisualDCT v2.7.1-SNAPSHOT

# % macro, P, Device Prefix
# % macro, R, Device Suffix
# % macro, DTYP, Device Type
# % macro, OUT, Output link
# % macro, SCAN, Scan Rate
# % macro, PREC, Precision
# % macro, EGUL, EGU low scale
# % macro, EGUF, EGU full scale
# % macro, DRVH, Drive high limit
# % macro, DRVL, Drive low limit
# % macro, EGU, EGU
# % macro, PINI, Process on iocInit
# % macro, DESC, Description
# % macro, LINR, LINR
# % macro, name, Object name and gui association name
# % macro, gda_name, GDA Name
# % macro, gda_desc, GDA Description
# % macro, archiver_rate, epics archiver configuration
# This associates an edm screen with the template
# % gui, USER.A14, edmembed, ao_embed.edl, P=$(dom,undefined)-EA-USER-01:A14,R=
# % gui, USER.A14, boydetail, userIOApp_opi/ao_detail.opi, P=$(dom,undefined)-EA-USER-01:A14,R=, desc=Analogue Output, DESC=Analogue Output
# % gui, USER.A14, boyembed, userIOApp_opi/ao_embed_box.opi, P=$(dom,undefined)-EA-USER-01:A14,R=, desc=Analogue Output, DESC=Analogue Output
# % gui, USER.A14, boyembed, userIOApp_opi/ao_embed.opi, P=$(dom,undefined)-EA-USER-01:A14,R=, desc=Analogue Output, DESC=Analogue Output
# % gui, USER.A14, demand, Set Value, $(dom,undefined)-EA-USER-01:A14
# % gui, USER.A14, groupHeading, Settings
# % gui, USER.A14, demand, EGU, $(dom,undefined)-EA-USER-01:A14.EGU
# % gui, USER.A14, readback, EGU, $(dom,undefined)-EA-USER-01:A14.EGU
# % gui, USER.A14, demand, EGU Low, $(dom,undefined)-EA-USER-01:A14.EGUL
# % gui, USER.A14, demand, EGU Full, $(dom,undefined)-EA-USER-01:A14.EGUF
# % gui, USER.A14, demand, Drive Low, $(dom,undefined)-EA-USER-01:A14.DRVL
# % gui, USER.A14, demand, Drive High, $(dom,undefined)-EA-USER-01:A14.DRVH
# % gui, USER.A14, demand, Precision, $(dom,undefined)-EA-USER-01:A14.PREC
# % gui, USER.A14, enum, Conversion Type, $(dom,undefined)-EA-USER-01:A14.LINR
# This makes the component icon reflect the severity
# % gui, USER.A14, sevr
#% gdatag,template,simplePv,USR1:A14,USR1 A14
#% gdatag,pv,rw,USR1:A14,RECORD
#% archiver 10
#% autosave 1 VAL
record(ao, "$(dom,undefined)-EA-USER-01:A14") {
  field(DTYP, "")
  field(OUT, "")
  field(SCAN, "")
  field(LINR, "")
  field(EGUF, "")
  field(EGUL, "")
  field(DRVH, "")
  field(DRVL, "")
  field(DESC, "Analogue Output")
  field(EGU, "V")
  field(PINI, "NO")
  field(PREC, "")
}
#! Generated by VisualDCT v2.7.1-SNAPSHOT

# % macro, P, Device Prefix
# % macro, R, Device Suffix
# % macro, DTYP, Device Type
# % macro, OUT, Output link
# % macro, SCAN, Scan Rate
# % macro, PREC, Precision
# % macro, EGUL, EGU low scale
# % macro, EGUF, EGU full scale
# % macro, DRVH, Drive high limit
# % macro, DRVL, Drive low limit
# % macro, EGU, EGU
# % macro, PINI, Process on iocInit
# % macro, DESC, Description
# % macro, LINR, LINR
# % macro, name, Object name and gui association name
# % macro, gda_name, GDA Name
# % macro, gda_desc, GDA Description
# % macro, archiver_rate, epics archiver configuration
# This associates an edm screen with the template
# % gui, USER.A15, edmembed, ao_embed.edl, P=$(dom,undefined)-EA-USER-01:A15,R=
# % gui, USER.A15, boydetail, userIOApp_opi/ao_detail.opi, P=$(dom,undefined)-EA-USER-01:A15,R=, desc=Analogue Output, DESC=Analogue Output
# % gui, USER.A15, boyembed, userIOApp_opi/ao_embed_box.opi, P=$(dom,undefined)-EA-USER-01:A15,R=, desc=Analogue Output, DESC=Analogue Output
# % gui, USER.A15, boyembed, userIOApp_opi/ao_embed.opi, P=$(dom,undefined)-EA-USER-01:A15,R=, desc=Analogue Output, DESC=Analogue Output
# % gui, USER.A15, demand, Set Value, $(dom,undefined)-EA-USER-01:A15
# % gui, USER.A15, groupHeading, Settings
# % gui, USER.A15, demand, EGU, $(dom,undefined)-EA-USER-01:A15.EGU
# % gui, USER.A15, readback, EGU, $(dom,undefined)-EA-USER-01:A15.EGU
# % gui, USER.A15, demand, EGU Low, $(dom,undefined)-EA-USER-01:A15.EGUL
# % gui, USER.A15, demand, EGU Full, $(dom,undefined)-EA-USER-01:A15.EGUF
# % gui, USER.A15, demand, Drive Low, $(dom,undefined)-EA-USER-01:A15.DRVL
# % gui, USER.A15, demand, Drive High, $(dom,undefined)-EA-USER-01:A15.DRVH
# % gui, USER.A15, demand, Precision, $(dom,undefined)-EA-USER-01:A15.PREC
# % gui, USER.A15, enum, Conversion Type, $(dom,undefined)-EA-USER-01:A15.LINR
# This makes the component icon reflect the severity
# % gui, USER.A15, sevr
#% gdatag,template,simplePv,USR1:A15,USR1 A15
#% gdatag,pv,rw,USR1:A15,RECORD
#% archiver 10
#% autosave 1 VAL
record(ao, "$(dom,undefined)-EA-USER-01:A15") {
  field(DTYP, "")
  field(OUT, "")
  field(SCAN, "")
  field(LINR, "")
  field(EGUF, "")
  field(EGUL, "")
  field(DRVH, "")
  field(DRVL, "")
  field(DESC, "Analogue Output")
  field(EGU, "V")
  field(PINI, "NO")
  field(PREC, "")
}
#! Generated by VisualDCT v2.7.1-SNAPSHOT

# % macro, P, Device Prefix
# % macro, R, Device Suffix
# % macro, DTYP, Device Type
# % macro, OUT, Output link
# % macro, SCAN, Scan Rate
# % macro, PREC, Precision
# % macro, EGUL, EGU low scale
# % macro, EGUF, EGU full scale
# % macro, DRVH, Drive high limit
# % macro, DRVL, Drive low limit
# % macro, EGU, EGU
# % macro, PINI, Process on iocInit
# % macro, DESC, Description
# % macro, LINR, LINR
# % macro, name, Object name and gui association name
# % macro, gda_name, GDA Name
# % macro, gda_desc, GDA Description
# % macro, archiver_rate, epics archiver configuration
# This associates an edm screen with the template
# % gui, USER.A16, edmembed, ao_embed.edl, P=$(dom,undefined)-EA-USER-01:A16,R=
# % gui, USER.A16, boydetail, userIOApp_opi/ao_detail.opi, P=$(dom,undefined)-EA-USER-01:A16,R=, desc=Analogue Output, DESC=Analogue Output
# % gui, USER.A16, boyembed, userIOApp_opi/ao_embed_box.opi, P=$(dom,undefined)-EA-USER-01:A16,R=, desc=Analogue Output, DESC=Analogue Output
# % gui, USER.A16, boyembed, userIOApp_opi/ao_embed.opi, P=$(dom,undefined)-EA-USER-01:A16,R=, desc=Analogue Output, DESC=Analogue Output
# % gui, USER.A16, demand, Set Value, $(dom,undefined)-EA-USER-01:A16
# % gui, USER.A16, groupHeading, Settings
# % gui, USER.A16, demand, EGU, $(dom,undefined)-EA-USER-01:A16.EGU
# % gui, USER.A16, readback, EGU, $(dom,undefined)-EA-USER-01:A16.EGU
# % gui, USER.A16, demand, EGU Low, $(dom,undefined)-EA-USER-01:A16.EGUL
# % gui, USER.A16, demand, EGU Full, $(dom,undefined)-EA-USER-01:A16.EGUF
# % gui, USER.A16, demand, Drive Low, $(dom,undefined)-EA-USER-01:A16.DRVL
# % gui, USER.A16, demand, Drive High, $(dom,undefined)-EA-USER-01:A16.DRVH
# % gui, USER.A16, demand, Precision, $(dom,undefined)-EA-USER-01:A16.PREC
# % gui, USER.A16, enum, Conversion Type, $(dom,undefined)-EA-USER-01:A16.LINR
# This makes the component icon reflect the severity
# % gui, USER.A16, sevr
#% gdatag,template,simplePv,USR1:A16,USR1 A16
#% gdatag,pv,rw,USR1:A16,RECORD
#% archiver 10
#% autosave 1 VAL
record(ao, "$(dom,undefined)-EA-USER-01:A16") {
  field(DTYP, "")
  field(OUT, "")
  field(SCAN, "")
  field(LINR, "")
  field(EGUF, "")
  field(EGUL, "")
  field(DRVH, "")
  field(DRVL, "")
  field(DESC, "Analogue Output")
  field(EGU, "V")
  field(PINI, "NO")
  field(PREC, "")
}
#! Generated by VisualDCT v2.7.1-SNAPSHOT

#% gdatag,template,simpleArray,USR1:WV1,USR1 WV1
#% gdatag,array,ro,USR1:WV1,RECORD
# This associates an edm screen with the template
# % gui, USER.WV1, edm, waveform_small.edl, P=$(dom,undefined)-EA-USER-01:WV1,R=
record(waveform, "$(dom,undefined)-EA-USER-01:WV1") {
  field(DTYP, "")
  field(FTVL, "FLOAT")
  field(NELM, "100000")
  field(INP, "")
  field(SCAN, "")
  field(DESC, "Array")
}
#! Generated by VisualDCT v2.7.1-SNAPSHOT

#% gdatag,template,simpleArray,USR1:WV2,USR1 WV2
#% gdatag,array,ro,USR1:WV2,RECORD
# This associates an edm screen with the template
# % gui, USER.WV2, edm, waveform_small.edl, P=$(dom,undefined)-EA-USER-01:WV2,R=
record(waveform, "$(dom,undefined)-EA-USER-01:WV2") {
  field(DTYP, "")
  field(FTVL, "FLOAT")
  field(NELM, "100000")
  field(INP, "")
  field(SCAN, "")
  field(DESC, "Array")
}
#! Generated by VisualDCT v2.7.1-SNAPSHOT

#% gdatag,template,simpleArray,USR1:WV3,USR1 WV3
#% gdatag,array,ro,USR1:WV3,RECORD
# This associates an edm screen with the template
# % gui, USER.WV3, edm, waveform_small.edl, P=$(dom,undefined)-EA-USER-01:WV3,R=
record(waveform, "$(dom,undefined)-EA-USER-01:WV3") {
  field(DTYP, "")
  field(FTVL, "FLOAT")
  field(NELM, "100000")
  field(INP, "")
  field(SCAN, "")
  field(DESC, "Array")
}
#! Generated by VisualDCT v2.7.1-SNAPSHOT

#% gdatag,template,simpleArray,USR1:WV4,USR1 WV4
#% gdatag,array,ro,USR1:WV4,RECORD
# This associates an edm screen with the template
# % gui, USER.WV4, edm, waveform_small.edl, P=$(dom,undefined)-EA-USER-01:WV4,R=
record(waveform, "$(dom,undefined)-EA-USER-01:WV4") {
  field(DTYP, "")
  field(FTVL, "FLOAT")
  field(NELM, "100000")
  field(INP, "")
  field(SCAN, "")
  field(DESC, "Array")
}
